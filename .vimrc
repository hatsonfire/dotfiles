set nocompatible

set backspace=indent,eol,start " Makes backspace behave as you'd expect
filetype plugin indent on      " Enable filetype detection and indentation
syntax on                      " Enable syntax highlighting
